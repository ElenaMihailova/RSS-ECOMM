// import { JSDOM } from 'jsdom';

// import { removeClass } from '../src/util/dom-functions';

// const dom = new JSDOM();
// global.document = dom.window.document;

// describe('check dom-functions', () => {
//   const elementMock = document.createElement('div');
//   elementMock.classList.add('mockclass');

//   test('check remove class function', () => {
//     removeClass([elementMock], 'mockclass');
//     expect(elementMock.classList.contains('mockclass')).toBe(false);
//   });
// });
